<div class="container mt-5">
<h1><%= @list.name %></h1>
  <div class="d-flex justify-content-evenly align-items-center mb-4">
    <% @movies.each do |movie| %>
      <div class="card" style="width: 18rem;">
        <img src="<%= movie.poster_url %>" class="card-img-top" alt="...">
        <div class="card-body">
          <h5 class="card-title"><%= movie.title %></h5>
          <p class="card-text"><%= movie.rating %>💛</p>
          <p class="card-text"><%= movie.overview %></p>
          <hr>
          <p class="card-text"><small class="text-muted">
            <% @list.bookmarks.each do |b| %>
                <% if b.movie_id == movie.id %>
                "<%= b.comment %>"
                <% end %>
            <% end %>
          </small></p>
        </div>
      </div>
    <% end %>
  </div>
    <%# <%= link_to edit_list_path(@list.id), class: 'btn btn-light btn-sm border' do %>
      <%# <i class="fa fa-pencil"></i> Edit this list
    <%# <% end %>
  <hr>
  <%= link_to lists_path do %>
    <i class="fa-solid fa-arrow-rotate-left"></i> Back to list
  <% end %>
  <br>
</div>
